hai this is aws fi;e
we are uploading this file to github
we use command loke git push -u origin master
addding data directly into github
public class AsciiValue {

    public static void main(String[] args) {

        char ch = 'a';
        int ascii = ch;
        // You can also cast char to int
        int cast " + ch + " is: " + castAscii);
    }
}
